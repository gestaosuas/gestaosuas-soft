-- CRAS Reports Comprehensive Migration
-- This guarantees that all fields in cras-config.ts exist in the DB.
ALTER TABLE cras_reports 
    ADD COLUMN IF NOT EXISTS mes_anterior INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS admitidas INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS desligadas INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS atual INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS atendimentos INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS visita_domiciliar INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS atend_particularizado INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS pro_pao INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS dmae INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS auxilio_documento INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS cesta_basica INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS fralda INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS absorvente INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS bpc INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS carteirinha_idoso INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS passe_livre_deficiente INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS cadastros_novos INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS recadastros INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS anexo_rma TEXT DEFAULT '';

-- Beneficios Reports Comprehensive Migration
-- This guarantees that all fields in beneficios-config.ts exist in the DB.
ALTER TABLE beneficios_reports 
    ADD COLUMN IF NOT EXISTS encaminhadas_inclusao_cadunico INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS encaminhadas_atualizacao_cadunico INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS consulta_cadunico INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS numero_nis INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS dmae INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS pro_pao INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS auxilio_documento INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS carteirinha_idoso INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS bpc_presencial INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS bpc_online INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS solicitacao_colchoes INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS cesta_basica INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS solicitacao_fraldas INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS absorvente INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS agasalho_cobertor INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS visitas_cadunico INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS visita_nucleo_habitacao INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS visita_cesta_fraldas_colchoes INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS visita_dmae INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS visitas_pro_pao INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS total_visitas INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS busao_social_1 INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS busao_social_2 INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS dibs INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS familias_pbf INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS pessoas_cadunico INTEGER DEFAULT 0;
